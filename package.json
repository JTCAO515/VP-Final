{
  "name": "visepanda",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "turbo run build",
    "evals": "pnpm evals:trip_generation",
    "evals:trip_generation": "node evals/run-trip-generation.mjs",
    "docs:index": "node scripts/docs/generate-index.mjs",
    "docs:check": "node scripts/docs/check.mjs",
    "docs:impact": "node scripts/docs/check-impact.mjs",
    "browser:install": "playwright install chromium",
    "test:browser": "playwright test --project=chromium",
    "test:browser:headed": "playwright test --project=chromium --headed",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "prettier": "^3.4.2",
    "turbo": "^2.3.3",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
